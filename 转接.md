# 三位一体辅助系统 · 交接文档（转接.md）

> 供新对话窗口快速接手。生成时间：2026-09-11（上一版 2026-09-10）
> 线上已部署 Cloudflare；**本地全部改动尚未 commit / push**（详见 §6）。
> **本版重点更新**：① 前端完成「控制台化」美术与布局大改版（侧边栏 + 顶栏 + 仪表盘 + 数据表格 + 深色模式，见 §7.5）；② 侧边栏重构为文档流内兄弟容器，窄屏叠压 / 折叠空间不回收已修复（见 §7.5）；③ **下一批任务已定稿**：分数口径统一（原始分 ｜ 换算分百分制）+ 开放数据乱码修复，**待执行**（见 §7.6）。

---

## 0. 三句话速览

1. **真实应用**在 `d:/Desktop/VibeCoding/三位一体辅助系统/live-app/`（React 单文件 + Node server + Cloudflare functions），本地跑 8080，线上已部署。
2. **GitHub 仓库** `lyhtakeashot/three-one-assistant` 跟踪的是外层目录；**线上站点** `https://three-one-assistant.pages.dev`（Cloudflare Pages + D1 + KV）树洞已真实互通。
3. ⚠️ 磁盘存在**两套**三位一体代码，只有 `live-app` 是当前维护版（详见 §2），**改代码请只改 live-app**。

---

## 1. 关键位置

| 项目 | 路径 / 值 |
|---|---|
| 目标文档 | `d:/Desktop/VibeCoding/三位一体辅助系统/三位一体辅助系统全目标.md` |
| **数据采集需求表** | `d:/Desktop/VibeCoding/三位一体辅助系统/三位一体数据采集需求表.md` |
| Git 仓库根（外层） | `d:/Desktop/VibeCoding/三位一体辅助系统/`（.git、README、.gitignore） |
| **真实应用（唯一维护版）** | `d:/Desktop/VibeCoding/三位一体辅助系统/live-app/` |
| GitHub 远程 | `https://github.com/lyhtakeashot/three-one-assistant.git`（分支 main） |
| 线上网址（稳定） | `https://three-one-assistant.pages.dev` |
| 本地运行 | `cd live-app && node server.cjs` → http://localhost:8080 |
| 本地服务状态 | **当前已在运行**（8080 端口监听中），页面标题校验通过 |

---

## 2. ⚠️ 双副本警示（务必先读）

| 目录 | 是什么 | 维护? |
|---|---|---|
| `.../live-app/` | **真实应用**：单文件前端 index.html + server.cjs + functions + PWA + test + open-data | ✅ 是，所有开发在这里 |
| `.../three-one-assistant/`（外层下） | 旧 Vite + React TS 工程（src/*.tsx、vite.config 等） | ❌ 不再维护（保留未动，勿删） |
| `d:/three-one-assistant`（D 盘根） | 迁移后的**空残留**，仅剩被占用删不掉的 `functions/` | 待手动删除 |

**历史教训**：曾因改错副本导致 GitHub 推错内容。新窗口接手务必确认 cwd 在 `live-app` 下。

---

## 3. live-app 结构

```
live-app/
├── index.html        # 前端全部（React 18 UMD 单文件 + 控制台 UI）：46所院校数据/工作台/院校库/详情/计算器/对比/收藏/档案/FAQ/树洞/纠错/开放数据 + 内联 CSS 设计系统 + 内联 SVG 图标表 + PWA meta + 8s 轮询
├── server.cjs        # 本地后端：静态服务 + /api/*（JSON 文件存 data/，8080）
├── functions/        # 【线上后端】Cloudflare Pages Functions（.js ESM）
│   ├── lib/pure.js   # 业务纯逻辑唯一来源（校验/排序/幂等/序列化），供 handler 与测试复用
│   ├── lib/http.js / posts.js
│   └── api/*.js      # treehole/counter/feedback + [id]/like|reply|pin|report + _middleware(CORS)
├── schema.sql        # D1 建表脚本（posts/feedback/meta）——已在线执行
├── sw.js / manifest.json / icon.*   # PWA
├── test/             # Node 原生零依赖测试（run-tests.cjs，约 4000 项）
├── open-data/        # 开放数据包（schools.json/csv/md + README，v2.0.0 / 46 所，含 majorsYear）
├── tools/export-data.cjs     # 重新生成 open-data 的脚本
├── tools/import-schools.cjs  # 导入脚本：schools_46.json（+ 合并 schools_2025.json）→ SCHOOLS（可 --dry-run）
├── docs/
│   ├── DEPLOY-CLOUDFLARE.md      # 云端部署/排查清单（重要参考）
│   └── MAJOR-ADMISSION-SOP.md    # 专业级录取线采集 SOP
├── data/             # 本地运行时 JSON（勿提交；当前为未跟踪）
└── src/、app.html 等  # Vite/旧版残片，不影响运行，可后续精简
```

---

## 4. 技术要点与约定

- **前端无构建**：React 18 从 unpkg UMD 全局加载，全部 `React.createElement` 内联，禁止 import/JSX。函数用 `function`/`var`。
- **数据落点**：院校数据写在 `index.html` 顶部的 `var SCHOOLS=[...]`（现 46 所）；`open-data/schools.json` 由 `tools/export-data.cjs` 同步。批量导入用 `tools/import-schools.cjs`。`live-app/data.js` 是旧 Vite 残片，index.html 未引用，勿再手工同步。
- **functions 是 ESM**（`.js` + 外层 package.json `type:module` 一致）。Cloudflare 会把 `functions/` 当函数目录，`[id]` 为动态路由。
- **纯逻辑单一来源**：`functions/lib/pure.js` 的规则（匿名递增/点赞幂等/置顶排序/分类白名单/长度 clamp）是唯一标准；`server.cjs`(本地) 与线上 handler 语义对齐，靠 `test/cf-pure.test.cjs` 保证不漂移。
- **存储分工（线上）**：D1 = posts/feedback/meta（事务）；KV = 访问计数。绑定变量名固定为 `DB` 与 `COUNTER_KV`（改绑定名需同步改 handler）。
- **命令行中文路径坑**：PowerShell 里不要用 `cd /d`（那是 cmd 语法）；shell 默认已在本项目根目录，直接用相对路径即可。传含中文参数给外部程序时若乱码，把脚本写在 ASCII 路径、内容里用 UTF-8 写中文路径再执行。
- 测试运行：`node live-app/test/run-tests.cjs`（支持 `node run-tests.cjs api` 单跑）。全套 **4013 项** = data3798 + calculator95 + html59 + render13 + api20 + cf-pure28（data 组按「标准/特殊折算」分级校验）。**任何改动后必须保持 0 failed**。
- **测试 mock 的硬约束（改前端前必读）**：`test/lib/mock-react.cjs` 只提供 `React.createElement/useState/useMemo/useEffect/useRef`；`window` 只有 `confirm/addEventListener`；`document` 只有 `getElementById/createElement/body`。⇒ **禁用** `useCallback/useContext/useReducer/Fragment/memo/forwardRef/useLayoutEffect`；**禁止在渲染路径或 `useState` 初始化函数里读 `window.innerWidth`/`matchMedia`**（mock 下会直接抛错）；响应式只能靠**纯 CSS media query + 类名/属性**。
- `test/lib/extract.cjs` 依赖两个切片标记：`var SCHOOLS=[` 与 `// === Components`，**必须原样保留**，且该区间（数据/Storage/API/Utils/年份纯函数）**不得引用 `window`/`document`**。导出的纯函数名与签名（含 `schoolLine/majorLine/admissionRow/schoolDataYear/hasCurrentData`）**不得改动**（calculator.test.cjs 直接断言其返回结构）。
- `test/render.test.cjs` 会对 13 个组件逐个 `eval(name)({setPage:()=>{}})` 并断言返回对象 `type!==undefined` ⇒ **13 个组件名必须保留**。
- `test/html.test.cjs` 含结构/样式字符串断言：脚本内不得出现 `bg-s8 text-w`；必须存在 `'bg-p50 text-p p3 text-center text-sm'`；必须存在 `ICON_PATHS/NAV_SECTIONS/PAGE_META/sidebar/topbar/drawer-mask/navMode/data-theme/data-table/--nav-w:248px/--nav-cur`，且**不得出现** `BOTTOM_NAV` 与 `bottom-nav`。改外壳结构时需同步这几条断言。

### 本轮新增的院校数据字段（index.html 已实际渲染）
- `formulaSource: {year, title, url}` — 折算规则官方来源，详情页/计算器展示依据
- `brochureUrl` — 招生简章链接，详情页「📄 招生简章」
- `major.admissionStats: {year, minScore, sourceUrl}` — 专业级三一最低综合分，计算器选专业后按此判冲稳保
- `majorsYear` — 该校 `majors` 名单的年份：`2026`=今年已发布；`2025`=今年未发布，名单与参考线回退去年
- `scoreSegments[]` — 一分一段，类型已定义但**尚未启用**

#### 「今年优先 / 去年回退」规则（计算器 + 详情页）
- 判据：学校 `admission` 是否含 ≥2026 的行；有→今年已发布（`majorsYear=2026`），无→今年未发布（`majorsYear=2025`，改用 `.ref/schools_2025.json` 的去年完整专业名单）。
- 当前 46 所中 **15 所今年已发布 / 31 所回退 2025**（含浙大等原 15 所；`eit` 2026 首招无去年数据，仍按今年）。
- 前端纯函数：`schoolDataYear` / `hasCurrentData` / `admissionRow` / `schoolLine` / `majorLine`（`test/lib/extract.cjs` 已暴露）；UI 标注「· 2025年数据」「⚠ 该校 2026 年招生信息尚未公布…」「（2025）」「2026 待公布，暂用 2025 年」。
- `majorsYear` 由 `tools/import-schools.cjs` 每次执行时重算，**勿手工修改**。

#### 计算器「学考等级」合计（本版新增）
- 新增纯函数 `xuekaoTotal(grades, formula)` → `{sum, percent, fullScore, selected}`；UI 在「学考等级」卡片底部显示两行：**学考折算分**（`sum / fullScore`，如 `25 / 150`）与 **百分制折算分**（`percent`）。
- 未选院校 / 一科未选 / E 等 / 无学考折算院校（`xuekao.A<=0` 或 `fullScore<=0`）→ 一律显示「—」；E 与未选不计分。
- 复用 `calcXuekao` / `xkPercent`，口径与结果区一致；`test/lib/extract.cjs` 已暴露 `xuekaoTotal`。

---

## 5. 云端部署现状（已上线 ✅）

| 项 | 值 |
|---|---|
| 平台 | Cloudflare Pages（git 集成，连 GitHub 仓库，push 自动部署） |
| Root directory | `live-app`；Build command 留空（勿跑 Vite） |
| D1 数据库 | 已建、已绑定（变量名 `DB`）、已执行 schema.sql（posts/feedback/meta 表） |
| KV | 已建、已绑定（变量名 `COUNTER_KV`） |
| 冒烟验证 | 2026-09-03 全通过：首页200 / counter / 发帖(匿名0001) / 点赞幂等 / 列表 / feedback / CORS |
| 已知修复 | functions `[id]/*` import 相对路径层级 4→3 层；`.mjs→.js` 因 CF 只扫 .js/.ts；PWA SW 改 network-first（v3）|

线上现留一条冒烟测试帖（匿名0001），如需清理给 D1 Console 一条 DELETE 即可。

---

## 6. git 历史（main）

```
e096a7f feat(calc): score inputs by school fullscale with clamp and live percent conversion; xiaokao normalized in pure fns; reverse dual-scale output
776f57d feat(calc): calc button only in forward mode (purple) vs reverse amber; err gated by mode
df797f4 feat(calc): grade select placeholder + must-complete validation, calc button moved into estimate card, tier label short 冲/稳/保
786857c feat(calc): per-major admission line UI + reverse target score card; add major admission SOP doc
95b7926 fix(pwa): network-first for html/navigation with cache v3; modal radius 12px + px6 header padding
bd4c3c8 fix(pwa): bump sw cache v2, auto reload on controllerchange, no-cache html on local server
86df07b fix(ui): modal overlay backdrop blur + opaque card for clear reading
c4f9eac feat(home): move 3-step flow into modal pill button beside intro; darken modal overlay to 0.8
f3fe161 fix(cloudflare): [id] import 路径 4层->3层
12bf403 feat(cloudflare): 迁移树洞至 Pages Functions + D1/KV … 应用移入 live-app
```

> **状态（2026-09-11 用 git status 核实）**：
> - 最新提交：`78ecc70 feat(schools): import 46 schools with 2025 major lists; prior-year fallback for unpublished schools; xuekao summary display` —— 「46 校导入 + 年份回退 + 学考合计」**已提交** ✅
> - **仍未提交的工作区改动**：`M live-app/index.html`（控制台 UI 大改版 + 侧边栏重构）、`M live-app/sw.js`（缓存版本已升到 `3in1-v6`）、`M live-app/test/html.test.cjs`（控制台结构断言）、`AM 转接.md`
> - 另有已 `git add` 尚未提交的资产：`.codebuddy/plans/`（含两个 `console-ui-overhaul...(未完成)` 计划）、`.workbuddy/`、`.三位一体数据采集表.ref/`、`三位一体数据采集表*.xlsx`、`三位一体数据采集需求表.md`、`live-app/data/counter.json`
> - 与 origin 的领先/落后请用 `git status -sb` 现场确认（本节只记录工作区状态）。计划：**§7.6 两项任务做完后一起 commit + push**（提交信息用 ASCII，PowerShell 中文乱码坑）。

---

## 7. 🎯 院校数据采集专项进度（当前主要工作）

### 目标
把院校数据库从现有 **15 所** 扩充到**浙江省三位一体 / 综合评价全部院校**。

### 2026 年官方名单口径（重要）

| 类别 | 数量 | 说明 |
|---|---|---|
| 省内地方属三位一体 | 32 所 | 2025 年为 39 所，2026 年减少 7 所 |
| 高水平三位一体 | 6 所 | 复旦、上海交大、浙大、国科大、西湖大学、宁波东方理工大学（后者 2026 首次招生）|
| 在浙综合评价 | 8 所 | 上海纽约、昆山杜克、北外、上科大、南科大、华南理工、港中深、深北莫 |
| **2026 合计** | **46 所** | 三位一体口径 = 32 + 6 = 38；含综合评价共 46 |
| 2026 未招（备查） | 7 所 | 台州学院、衢州学院、宁波工程学院、温州理工学院、金华职业技术大学、宁波幼儿师范高专、湖州学院 |

- 官方来源：浙江省教育考试院《2026 三位一体招生开启！报考要点速览》（2026-03-05）
- 当前已收录 **46 所**（省内地方属 32 + 高水平 6 + 综合评价 8），导入已完成 ✅（详见下方「导入结果」）。

### 已产出的文件（均在项目根目录，**未提交 git**）

| 文件 / 目录 | 说明 |
|---|---|
| `三位一体数据采集需求表.md` | **字段规范文档**：16 字段块 + 4 张表模板 + 格式约定 + 采集优先级 + JSON 模板 |
| `三位一体数据采集表.xlsx` | **2026 采集表**：表A 基础信息 / 表B 招生专业 / 表C 历年竞争比 / 表D 报名流程 + 院校清单 + 折算规则表 + 使用说明。已预填 15 所、预留 31 行 |
| `三位一体数据采集表-2025.xlsx` | **2025 历史对照表**：52 所（39 + 5 + 8） |
| `.三位一体数据采集表.ref/` | **生成脚本与中间数据（隐藏目录，勿删）**，见下表 |

`.三位一体数据采集表.ref/` 内容：

| 文件 | 作用 |
|---|---|
| `build.py` | 生成 2026 采集表（源：`open-data/schools.json` 15 所 + 官方名单） |
| `build46.py` | 合并 `open-data`(15) + `extra_1/2/3.json`(31) + `satisfaction_G/H.json` → **`schools_46.json`** → 重建 2026 表 |
| `build2025.py` | 生成 2025 表；合并 `schools_46.json` + `y2025_*.json` → **`schools_2025.json`** |
| `add_calc.py` | 为两个工作簿各追加一张「折算规则表」（一校一行：三项比例 + 学考等级分值）|
| `schools_46.json` | **核心资产**：2026 年 46 所院校全字段数据（263KB）|
| `schools_2025.json` | 2025 年 52 所院校全字段数据（394KB）|
| `extra_0~3.json` | 31 所新增院校补充数据 |
| `y2025_1~7.json` | 2025 年度字段（formula/examFormat/majors/admission 等）分片 |
| `satisfaction_G/H.json` | 满意度数据 |
| `_jxnh_tmp.pdf` | 嘉兴南湖学院来源 PDF |

### 数据资产完整度（已核验）

`schools_46.json`：**46/46 所**均含 `formula` / `admission` / `majors` / `dormitory`；**46/46** 带 `formulaSource` 官方来源链接；43 所有满意度；25 所带 `formulaNote`（说明非标准折算口径，如「学考满分 100、D 计 2 分」等，**不是错误标记**）。

### ✅ 导入结果（2026-09-10 完成）

1. **导入已执行**：`schools_46.json` 归一化后写入 `live-app/index.html` 的 `var SCHOOLS=[...]`（15 → 46 所，原 15 所按 2026 章程覆盖）；`open-data/` 已重跑，版本升至 **v2.0.0 / 46 所**。
   - 注：`live-app/data.js` 为未被 index.html 引用的旧 Vite 残片（见 §8），本次未同步。
2. **导入脚本**：新增 `live-app/tools/import-schools.cjs`（可重复执行，支持 `--dry-run`）。归一化规则：`不限/无`→`[]`、`思想政治`→`政治`、选科仅留白名单、`admission` 缺失人数置 null 且空行剔除、缺满意度置 null、有笔试无科目补「综合知识」、缺失电话/学费补「暂未公布」。折算规则原样保留，不伪造数值。
3. **前端降级**：新增 `isStandardFormula()` 判定；折算口径特殊院校（`ucas/nyush/dku/bfsu/shtech/westlake/scut/cuhksz` 等）在详情页与计算器显示「折算口径特殊/以章程为准」提示，`calcResult`/`reverseGk`/`reverseXs` 做防除零，杜绝 NaN；缺失值统一渲染「暂无」。
4. **测试已过**：`node live-app/test/run-tests.cjs` → **3995 passed, 0 failed**；`data.test.cjs` 按「标准/特殊折算」分级校验，可空字段允许 null。
5. **年份回退（本版新增）**：`import-schools.cjs` 合并 `.ref/schools_2025.json`，对「今年未发布」的 31 所学校改用 2025 完整专业名单并写 `majorsYear`；计算器与详情页按「今年优先、去年回退」展示并标注年份（详见 §4）。
6. **学考合计展示（本版新增）**：计算器「学考等级」模块底部新增「学考折算分 / 百分制折算分」两行实时合计，未选 / E 等 / 无学考折算院校显示「—」（详见 §4）。

### 🚧 剩余待办

- [ ] 人工抽查 25 所 `formulaNote` 院校的折算口径（页面已标注「以章程为准」）。
- [ ] 优先核对「第一批·计算器可用」字段：历年最低综合分、学考门槛。
- [ ] 可选：把 `.ref/`、`.xlsx` 等数据资产纳入 git 或加入 `.gitignore`。

### ⚠️ 注意
- `三位一体数据采集表*.xlsx`、`.三位一体数据采集表.ref/`、`三位一体数据采集需求表.md` **均未纳入 git**，是否提交 / 加 `.gitignore` 待定。
- `.workbuddy/` 为工具临时目录，未跟踪，可忽略。

---

## 7.5 控制台 UI 大改版（已完成 · 2026-09-11）

把前端从「移动优先单页」改成「桌面控制台」（参考 BossHunter 布局，只借布局不借技术）。**仍严格单文件无构建**：`index.html` 内联 CSS + React.createElement，图标全部内联 SVG，零外部资源。

### 已完成内容

| 项 | 说明 |
|---|---|
| 外壳 | `.console`（flex 行）内含 `.sidebar` + `.console-main`（`.topbar` 吸顶 + `main.content` + footer） |
| 侧边栏 | 品牌区（三一 logo + 「数据控制台 · 浙江 2026」）→ `NAV_SECTIONS` 4 组 11 项分组导航（含收藏角标）→ 底部状态行 + 帮助/录取流程/折叠按钮 |
| 顶栏 | 当前页标题（`PAGE_META` 12 个路由）+ 副标题 + 状态点 + `2026 · 46 校` chip + 深色切换 + 纠错入口 |
| 仪表盘 | 工作台：鼓励语横幅（`ENCOURAGE_DATA` + 换一句）、4 张统计卡、6 个快捷入口、数据覆盖度漏斗、分数分布柱状图、最近动态、提前批提醒 |
| 数据表格 | `.data-table`（表头 sticky、可排序、`.num` 右对齐、窄屏降级为卡片，用 `.only-desktop/.only-mobile`） |
| 图标 | `ICON_PATHS`（约 60 个 lucide 风格线性图标，24×24 / stroke 2 / currentColor）+ `function Icon(props)` |
| 深色模式 | 手动切换：根 div `data-theme` + `localStorage.theme`，`:root` 与 `[data-theme="dark"]` 两套 token |
| 新增纯函数 | `dashboardStats` / `scoreBuckets` / `schoolSortVal` / `sortSchools` / `tierChip` / `yearChip` |
| PWA | `sw.js` 的 `CACHE_NAME` 已升到 `3in1-v6`（改完 index.html 要再升版本） |
| 测试 | 4013 passed / 0 failed |

### 侧边栏布局（2026-09-11 二次重构，务必理解）

- **根因**：原实现侧边栏 `position:fixed` 脱离文档流、主区靠 `margin-left` 手动跟随，两套数值在断点/折叠时失配 → 窄屏侧边栏压在主区上、收起时空间不回收。
- **现在的做法**：侧边栏是**文档流内的兄弟容器**（`position:sticky; top:0; align-self:flex-start; flex:0 0 auto; width:var(--nav-cur); height:100vh`），主区 `flex:1 1 auto; min-width:0`，**彻底不用 margin-left** → 结构上不可能重叠，收起时主区自动回填（只过渡 `width`）。
- **形态由根 div 的 `data-nav` 三态驱动**（localStorage 键 `navMode`，取值 `auto/open/closed`）：
  - `≥1200px` 默认 → 248px 完整侧边栏（分组标题 + 文字标签）
  - `768–1199px` 默认 → 68px 图标条（`.console:not([data-nav="open"])` 规则组）
  - `<768px` → 抽屉（`.sidebar{position:fixed;transform:translateX(-100%)}` + `.console.nav-open` + `.drawer-mask` + 汉堡按钮）
  - 手动：`data-nav="open"` 强制 248px、`"closed"` 强制 68px；侧边栏底部两个按钮按形态显隐（`.only-expanded` 收起 / `.only-rail` 展开，纯 CSS 判定）
- **坑**：图标条的「隐藏文字 + 图标居中」规则**必须限定 `≥768px`**，否则手机抽屉会被压成 68px 图标条。

---

## 7.6 🎯 下一批任务（已定稿，**待执行**）

用户 2026-09-11 提出，需求已逐条确认完毕，直接照做即可。

### 任务 1：最低综合分改为「原始分 ｜ 换算分（百分制）」统一两栏

**问题**：院校库「最低综合分」格里 `分数` 与 `年份` 挤在同一行（`<span class="cell-sub">` 在行内不走 block）；且分数混了两套口径无法对比。

**已确认的决策（勿再改）**
1. 换算口径：值 **>100 视为 750 分制 ÷7.5**（641→85.5）；**≤100 视为百分制原样保留**；**两者都同时显示「原始分 + 换算分」**。
2. 显示形式：**统一两栏**（所有院校都固定显示「原始分 ｜ 百分制」，允许百分制院校重复）。
3. 年份：**直接从分数格移除**（表格右侧已有独立「数据年份」列）。
4. 应用范围：**院校库移动卡片、院校详情页、我的收藏（表格+卡片）、计算器参考线** 全都要同步。

**已核实的数据事实**（用 node 实测 `SCHOOLS`）
- `admission[]` 字段：`{year, applicants, passed, admitted, minScore, xuekaoRequirement}`，53 行中 37 行有 `minScore`。
- minScore 只有两套：**≤100 共 29 条**（65 / 66.822 / 67 … 85 / 86）与 **595–648 共 8 条**（上纽 602、昆杜 610、港中深 614、南科大 639、华工 641、北外 643.62、上科大 648、深北莫 595）。
- 数据里的「120 分制」是部分院校的**校测满分**（`formula.xiaokao.fullScore` 含 100/120/150/225/250/300/750），**不出现在最低综合分列**；`formula.gaokao.fullScore` 全为 750。
- `majors[].admissionStats` 目前**无任何一条带 minScore**（0 行）。

**实现方式**
- 新增**独立纯函数**（放进切片区间、紧邻 `schoolLine`）：`scoreScaleMax(score, school)`（>100 → `school.formula.gaokao.fullScore || 750`，否则 100）、`toPercentScore(score, school)`（`Math.round(score/max*1000)/10`，非数字返回 null）。
- 新增顶层展示组件 `ScorePair(props)`（`{score, school, year}`）：左原始分（主字重）+ 右百分制小 chip；整格 `title` 提示「{year} 年 · 原始分 x / 满分 y」，避免丢信息。
- ⚠️ **不得修改 `schoolLine/majorLine/admissionRow` 的签名与返回结构**（`calculator.test.cjs` 直接断言返回值），换算只能靠新函数叠加。

**落地位置（行号基于 2026-09-11 状态）**

| 位置 | 行号 |
|---|---|
| 院校库表格「最低综合分」格（改两栏 + 移除年份） | index.html L940 |
| 院校库移动端卡片 | L966 |
| 详情页「最低综合分」StatCard | L1057 |
| 计算器参考线文案 | L1215 |
| 收藏页表格 | L1269 |
| 收藏页卡片 | L1283 |

**连带修正（同一口径问题，默认一并做）**
- `schoolSortVal(s,'line')` 目前按**原始分**排序 → 8 所 600+ 院校会恒排最前；建议改为按换算后的百分制排序。
- `scoreBuckets(schools)`（工作台分数分布柱状图）按**原始分**分桶 → 这 8 所会被错误计入 `≥90`；建议改为按百分制分桶。
- 若用户不想动排序/分桶，可只做显示层改动。

**验收**：院校库该列显示形如 `641 ｜ 85.5`／`80 ｜ 80.0`，年份不再挤在分数旁；6 处展示口径一致；排序与分桶符合百分制；桌面/移动、深浅色均正常。

### 任务 2：开放数据乱码修复

**根因（已实测，两处）**
1. `server.cjs` L3 的 mime 表**缺 `csv`/`md`** → L57 兜底 `'text/plain'`（**无 charset**）；实测 `curl -I http://localhost:8080/open-data/schools.csv` 与 `schools.md` 均返回 `Content-Type: text/plain`，浏览器按 GBK 解码 → 乱码。
2. `index.html` DownloadPage（L1714 起）的 `buildCsv()`（L1722）/`buildMd()`（L1729）生成的内容**没有 UTF-8 BOM**；`downloadFile`（L1738）虽然给 Blob 带了 `charset=utf-8`，但本地文件被 Excel/WPS 打开时不认 → CSV 乱码。
3. `tools/export-data.cjs`（L90-93）四个文件均以 `'utf8'` 写出、**无 BOM**。
> 用户反馈：**A（下载的 schools.csv 用 Excel/WPS 打开乱码）与 C（schools.md 乱码）都有问题**。
> `live-app/src/lib/export.ts` 是旧 Vite 残片，与本单文件应用无关，不在范围内。

**改法**
1. `server.cjs`：mime 表补 `csv:'text/csv;charset=utf-8'`、`md:'text/markdown;charset=utf-8'`；兜底改 `'text/plain;charset=utf-8'`。
2. `index.html` DownloadPage：`buildCsv()`/`buildMd()` 返回值前置 `'\uFEFF'`；**JSON 不加 BOM**（避免破坏解析）。
3. `tools/export-data.cjs`：写 `schools.csv` / `schools.md` 时前置 BOM，然后执行 `node tools/export-data.cjs` **重新生成 `open-data/`**。

**验收**：`curl -I` 看到 `charset=utf-8`；文件头 3 字节为 `EF BB BF`；浏览器直接访问 `/open-data/schools.csv`、`/open-data/schools.md` 中文正常；下载的 CSV 用 Excel 打开中文正常。

### 收尾（两个任务做完后）

- `sw.js` 的 `CACHE_NAME` 由 `3in1-v6` 升到 `3in1-v7`。
- 跑 `node live-app/test/run-tests.cjs` 必须 **0 failed**（`open-data` 测试只校验文件存在，加 BOM 安全）。
- 预览逐页核对：院校库桌面/移动、详情页、收藏页、计算器、开放数据页（下载 + 直接访问）、深浅主题。
- 本次仍**不自动 commit/push**（用户未要求）。

---

## 8. 待办与遗留（新窗口可选继续）

- [x] **【已完成】** 按 §7 完成院校数据导入（46 所 → SCHOOLS，含导入脚本与前端降级）
- [x] **【已完成】** 前端「控制台化」美术与布局大改版（侧边栏 + 顶栏 + 仪表盘 + 数据表格 + 深色模式，见 §7.5）
- [x] **【已完成】** 侧边栏重构为文档流内兄弟容器，修复窄屏叠压与折叠空间不回收（见 §7.5）
- [ ] **【下一批 · 已定稿待执行】** 见 §7.6：① 最低综合分改为「原始分 ｜ 换算分（百分制）」统一两栏（6 处展示 + 排序/分桶口径）；② 开放数据乱码修复（server.cjs 补 charset + 下载页与导出脚本给 CSV/MD 加 BOM + 重生成 open-data）
- [ ] **【优先】提交并推送本版改动**（控制台改版 + 侧边栏重构 + 46 校导入 + 年份回退 + 学考合计 + §7.6 两项）；push 自动触发 Cloudflare 部署。提交信息用 ASCII（PowerShell 中文乱码坑）
- [ ] 删除残留 `d:/three-one-assistant/`（重启 IDE 后手动删；当前只占 `functions/`）
- [ ] 精简 live-app 内 Vite 残片（`src/`、`app.html`、`components.js`、`pages.js`、`main.js`、`utils.js`、`style.css`、`data.js`、`vite.config.ts`、`package*.json`、`scripts/`、`public/`、`.github/`、`.env.example`）——删除需再 commit+push
- [ ] 大目标未做项：一分一段表、专业毕业去向参考、报名证书库参考（全目标文档第二组）
- [ ] 可选：绑定自定义域名；线上测试帖清理
- [ ] 提交并推送本地领先的 1 个提交（e096a7f）
- [ ] 服务器（本地8080）如未运行，`cd live-app && node server.cjs`

---

## 9. 新窗口第一步建议

```bash
# 1) 进入外层 git 根（shell 默认已在此目录，直接用相对路径）
#    注意：PowerShell 里不要用 cd /d 或 dir /s，那是 cmd 语法

# 2) 本地起服务（如需预览）
cd live-app && node server.cjs        # → http://localhost:8080

# 3) 跑测试
node live-app/test/run-tests.cjs

# 4) 接手任务时先读：
#    - 转接.md（本文件）§7.6（下一批任务，需求已确认，直接照做）
#    - 转接.md §7.5（控制台 UI 现状 + 侧边栏 data-nav/--nav-cur 机制）与 §4（技术约定 + 测试 mock 硬约束）
#    - 三位一体数据采集需求表.md
#    预览要点：桌面控制台（侧边栏折叠记忆、深色切换、院校库数据表格可排序）；窗口拉到 1100px 会自动收成图标条，<768px 变抽屉

# 5) 改动提交
git add live-app/... && git commit -m "..." && git push   # push 自动触发 Cloudflare 部署
```
